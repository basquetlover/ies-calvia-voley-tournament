---
interface Props {
    class?: string
}

const props: Props = Astro.props;
const { class: ClassAdicional } = props;
---
<section class={`max-w-3xl h-auto m-auto my-10 ${ClassAdicional}`}>
    <slot />
</section>