---
import Layout from "@layouts/Layout.astro";
---

<Layout title="">
    <section class="max-w-6xl h-auto grid grid-cols-2 grid-rows-1 gap-5 place-items-center m-auto">
        <img src="img/404.png" alt="error 404"/>
        <div class="flex flex-col items-center justify-center">
            <h4 class="text-accent text-4xl text-center">PÀGINA NO ENCONTRADA</h4>
            <p class="text-2xl text-center text-blanco">¡Hola! Lo sentimos, pero no pudimos encontrar lo que buscabas. Verifica que la dirección URL sea correcta.</p>
            <a href="/" class="h-max w-max py-2 px-4 mt-10 text-2xl bg-amarillo rounded-full text-azul font-medium text-center hover:scale-110 transition-all ">Volver al inicio</a>
        </div>
    </section>
</Layout>

<!--
<style>
    section{
        width: 60%;
        height: 100%;

        display: grid;
        gap: 20px;
        grid-template-columns: 1fr 1fr;
        grid-template-rows: 1fr;
        place-items: center;
        grid-template-rows: auto;
    }

    .info-error{
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        h4{
            font-size: 40px;
            color: var(--negro);
            text-align: center;
        }
        p{
            text-align: center;
            font-size: 20px;
            color: var(--negro);
        }
    }
    a{
        margin-top: 40px;
        padding: 15px;
        height: max-content;
        width: max-content;
        border: none;
        border-radius: 15px;
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        background-color: var(--azul_oscuro);
        transition: 0.3s;
        font-size: 20px;
        color: #0F191E;
        
    }
    
    a:hover{
        background-color: #33E0E5;
        transition: 0.3s;
    }
</style>
-->